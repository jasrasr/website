<?php
declare(strict_types=1);
// Documentation only: the application does not load this filename.
// Prefer copying your EXISTING server configuration to config.private.php,
// only if that destination does not already exist. Preserve the full users array.
// This example inherits existing users; it does not seed or replace accounts.
return [
    'authentication' => 'legacy', // Change only after links and permissions are verified.
];
