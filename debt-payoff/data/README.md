# Debt Payoff Planner Data

Runtime JSON files are created here locally on first run and are intentionally ignored by Git.

- `accounts.json` stores account metadata and password hashes.
- `users/*.json` stores each user's debt dataset.
- `audit/*.json` stores per-user audit logs.
- `backups/*/*.json` stores per-user backup snapshots.
